// Array containing allowed origins for CORS
const allowedOrigins = [
    '*', // All origins are allowed (change '*' to specific origins as needed)
];

// Exporting the allowedOrigins array for use in other modules
module.exports = allowedOrigins;
